<div class="container bg-shop text-center">
    <br><br><br><br><br><br>
    <h1>謝謝您的訂購。</h1>
    <br><br><br><br><br><br>
</div>

<div class= "container order_font bg-shop-2">
  <div class="row">
    <div class="col-md-12">

      <h2> 訂單明細 </h2>
      <table class="table table-bordered" >
        <thead>
          <tr>
            <th>商品明細</th>
            <th>價格：</th>
          </tr>
        </thead>

        <tbody>
        <% @order_items.each do |order_item| %>
            <tr>
              <td><%= order_item.product_title %></td>
              <td><%= @order.total %>NTD</td>
            </tr>
          <% end %>
        </tbody>
      </table>

       <hr>
       <h2> 寄送資訊 </h2>
       <table class="table table-bordered">
        <thead>
          <tr>
            <td>收件人</td>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>名字：<%= @order_info.shipping_name %></td>
          </tr>
          <tr>
            <td>地址：<%= @order_info.shipping_address %></td>
          </tr>
        </tbody>
      </table>
      
      <div class="pull-right" style="padding: 10px;">
          <%= render "pay_with_atm", order: @order %>
      </div>
      <div class="pull-right" style="padding: 10px;">
         <%= link_to( "修改資訊", edit_order_path, class: "btn white") %>
      </div>
    </div>
  </div>
</div>